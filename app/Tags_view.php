<?php
// This script and data application were generated by AppGini 22.11
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/Tags.php');
	include_once(__DIR__ . '/Tags_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('Tags');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'Tags';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`Tags`.`id`" => "id",
		"`Tags`.`Tag`" => "Tag",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`Tags`.`id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`Tags`.`id`" => "id",
		"`Tags`.`Tag`" => "Tag",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`Tags`.`id`" => "ID",
		"`Tags`.`Tag`" => "Tag",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`Tags`.`id`" => "id",
		"`Tags`.`Tag`" => "Tag",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`Tags` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'Tags_view.php';
	$x->RedirectAfterInsert = 'Tags_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Tags';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`Tags`.`id`';

	$x->ColWidth = [150, ];
	$x->ColCaption = ['Tag', ];
	$x->ColFieldName = ['Tag', ];
	$x->ColNumber  = [2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/Tags_templateTV.html';
	$x->SelectedTemplate = 'templates/Tags_templateTVS.html';
	$x->TemplateDV = 'templates/Tags_templateDV.html';
	$x->TemplateDVP = 'templates/Tags_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: Tags_init
	$render = true;
	if(function_exists('Tags_init')) {
		$args = [];
		$render = Tags_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: Tags_header
	$headerCode = '';
	if(function_exists('Tags_header')) {
		$args = [];
		$headerCode = Tags_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: Tags_footer
	$footerCode = '';
	if(function_exists('Tags_footer')) {
		$args = [];
		$footerCode = Tags_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
