<?php
// This script and data application were generated by AppGini 22.11
// Download AppGini for free from https://bigprof.com/appgini/download/

	include_once(__DIR__ . '/lib.php');
	@include_once(__DIR__ . '/hooks/Midia.php');
	include_once(__DIR__ . '/Midia_dml.php');

	// mm: can the current member access this page?
	$perm = getTablePermissions('Midia');
	if(!$perm['access']) {
		echo error_message($Translation['tableAccessDenied']);
		exit;
	}

	$x = new DataList;
	$x->TableName = 'Midia';

	// Fields that can be displayed in the table view
	$x->QueryFieldsTV = [
		"`Midia`.`id`" => "id",
		"`Midia`.`file`" => "file",
	];
	// mapping incoming sort by requests to actual query fields
	$x->SortFields = [
		1 => '`Midia`.`id`',
		2 => 2,
	];

	// Fields that can be displayed in the csv file
	$x->QueryFieldsCSV = [
		"`Midia`.`id`" => "id",
		"`Midia`.`file`" => "file",
	];
	// Fields that can be filtered
	$x->QueryFieldsFilters = [
		"`Midia`.`id`" => "ID",
		"`Midia`.`file`" => "File",
	];

	// Fields that can be quick searched
	$x->QueryFieldsQS = [
		"`Midia`.`id`" => "id",
		"`Midia`.`file`" => "file",
	];

	// Lookup fields that can be used as filterers
	$x->filterers = [];

	$x->QueryFrom = "`Midia` ";
	$x->QueryWhere = '';
	$x->QueryOrder = '';

	$x->AllowSelection = 1;
	$x->HideTableView = ($perm['view'] == 0 ? 1 : 0);
	$x->AllowDelete = $perm['delete'];
	$x->AllowMassDelete = (getLoggedAdmin() !== false);
	$x->AllowInsert = $perm['insert'];
	$x->AllowUpdate = $perm['edit'];
	$x->SeparateDV = 1;
	$x->AllowDeleteOfParents = 0;
	$x->AllowFilters = 1;
	$x->AllowSavingFilters = (getLoggedAdmin() !== false);
	$x->AllowSorting = 1;
	$x->AllowNavigation = 1;
	$x->AllowPrinting = 1;
	$x->AllowPrintingDV = 1;
	$x->AllowCSV = 1;
	$x->RecordsPerPage = 10;
	$x->QuickSearch = 1;
	$x->QuickSearchText = $Translation['quick search'];
	$x->ScriptFileName = 'Midia_view.php';
	$x->RedirectAfterInsert = 'Midia_view.php?SelectedID=#ID#';
	$x->TableTitle = 'Midia';
	$x->TableIcon = 'table.gif';
	$x->PrimaryKey = '`Midia`.`id`';

	$x->ColWidth = [150, ];
	$x->ColCaption = ['File', ];
	$x->ColFieldName = ['file', ];
	$x->ColNumber  = [2, ];

	// template paths below are based on the app main directory
	$x->Template = 'templates/Midia_templateTV.html';
	$x->SelectedTemplate = 'templates/Midia_templateTVS.html';
	$x->TemplateDV = 'templates/Midia_templateDV.html';
	$x->TemplateDVP = 'templates/Midia_templateDVP.html';

	$x->ShowTableHeader = 1;
	$x->TVClasses = "";
	$x->DVClasses = "";
	$x->HasCalculatedFields = false;
	$x->AllowConsoleLog = false;
	$x->AllowDVNavigation = true;

	// hook: Midia_init
	$render = true;
	if(function_exists('Midia_init')) {
		$args = [];
		$render = Midia_init($x, getMemberInfo(), $args);
	}

	if($render) $x->Render();

	// hook: Midia_header
	$headerCode = '';
	if(function_exists('Midia_header')) {
		$args = [];
		$headerCode = Midia_header($x->ContentType, getMemberInfo(), $args);
	}

	if(!$headerCode) {
		include_once(__DIR__ . '/header.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/header.php');
		echo str_replace('<%%HEADER%%>', ob_get_clean(), $headerCode);
	}

	echo $x->HTML;

	// hook: Midia_footer
	$footerCode = '';
	if(function_exists('Midia_footer')) {
		$args = [];
		$footerCode = Midia_footer($x->ContentType, getMemberInfo(), $args);
	}

	if(!$footerCode) {
		include_once(__DIR__ . '/footer.php'); 
	} else {
		ob_start();
		include_once(__DIR__ . '/footer.php');
		echo str_replace('<%%FOOTER%%>', ob_get_clean(), $footerCode);
	}
